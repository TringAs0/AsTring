<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.as0">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.As0">
        <activity android:name=".chat.ChatRoomListActivity"></activity>
        <activity android:name=".login.LoginActivity" />
        <activity android:name=".chat.ChatActivity" />
        <activity android:name=".login.VerifyOTPActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".VActivity" />
        <activity android:name=".splash.MainSplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>